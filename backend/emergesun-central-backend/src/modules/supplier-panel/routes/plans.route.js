const express = require('express');
const router = express.Router();

// Placeholder for subscription plans routes
router.get('/', (req, res) => res.json({ message: 'Subscription plans list' }));

module.exports = router;
